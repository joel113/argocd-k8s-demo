apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-demo
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: app
          image: ghcr.io/your-org/argocd-demo:test
          env:
            - name: APP_ENV
              value: "test"
